



  let chances = [10, 20, 30, 8, 9, 11, 12, 13, 14, 15];
  let map1 = chances.map(x => x * 3);
  function tripleTheChances(chances) {
  
  return map1;
  

  
  
  }
console.log(tripleTheChances());
  
/*const Valores = [3, 7, 10, 25]

// Nesse caso o map vai pegar os index da Array 'Valores' e vai multiplicar um por um por 2 retornando uma nova array com os valores multiplicados.
Valores.forEach(element => console.log(element * 2))*/


/*var numbers = [1, 2, 3, 4, 5];

for (var i = 0; i < numbers.length; i++) {
  numbers[i] *= 2;
}
// numbers is now [2, 4, 6, 8, 10]
console.log(numbers);*/