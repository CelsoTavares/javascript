const carro = {
    marca: 'Fiat',
    ano: 1998,
    modelo: 'Uno',
    dados: () => `A marca ${this.marca} com ano ${this.ano} e modelo ${this.modelo}`
    
}
console.log(carro.dados())
console.log(Date())
